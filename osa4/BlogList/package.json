{
  "name": "bloglist",
  "version": "0.0.1",
  "description": "bloglist app",
  "license": "MIT",
  "author": "Matti Reijonen",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node index.js",
    "dev": "cross-env NODE_ENV=development node --watch index.js",
    "test": "cross-env  NODE_ENV=test node --test",
    "devtest": "cross-env NODE_ENV=test node --watch index.js"
  },
  "dependencies": {
    "cross-env": "^10.1.0",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "mongoose": "^9.1.5"
  },
  "devDependencies": {
    "supertest": "^7.2.2"
  }
}
